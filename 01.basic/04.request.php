<?php

// La clase Request permite acceder a datos de la peticion realizada

/*
    Route::get('/data', function(Request $request) {
        return response($request->name);
    });
*/

// $request es una instancia de la clase Request
// En el ejemplo anterior se muestra por pantalla el valor del parametro name de la consulta realizada